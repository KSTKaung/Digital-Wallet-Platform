# ER Diagrams — Banking Digital Wallet Platform

Database-per-service means there are **no physical foreign keys across services** — a `wallet_id` referenced in `transfer_db` is a logical reference only, resolved via API calls or event payloads, never a SQL join. Each diagram below is one physically real, independently deployed PostgreSQL database. A cross-service logical reference map follows at the end.

---

## 1. `identity_db` — Identity Service

```mermaid
erDiagram
    USERS ||--o{ CREDENTIALS : has
    USERS ||--o{ REFRESH_TOKENS : has
    USERS ||--o{ SESSIONS : has
    USERS ||--o{ OTP_VERIFICATIONS : has

    USERS {
        uuid id PK
        varchar email UK
        varchar phone_number UK
        varchar status
        boolean email_verified
        boolean phone_verified
        timestamp created_at
        timestamp updated_at
    }
    CREDENTIALS {
        uuid id PK
        uuid user_id FK
        varchar password_hash
        timestamp password_changed_at
        int failed_attempts
        timestamp locked_until
        timestamp created_at
        timestamp updated_at
    }
    REFRESH_TOKENS {
        uuid id PK
        uuid user_id FK
        varchar token_hash UK
        timestamp expires_at
        timestamp revoked_at
        timestamp created_at
    }
    SESSIONS {
        uuid id PK
        uuid user_id FK
        varchar device_id
        inet ip_address
        text user_agent
        timestamp expires_at
        timestamp revoked_at
        timestamp created_at
    }
    OTP_VERIFICATIONS {
        uuid id PK
        uuid user_id FK
        varchar otp_hash
        varchar purpose
        timestamp expires_at
        timestamp verified_at
        timestamp created_at
    }
```

---

## 2. `customer_db` — Customer & KYC Service

```mermaid
erDiagram
    CUSTOMERS ||--o{ KYC_APPLICATIONS : submits
    KYC_APPLICATIONS ||--o{ KYC_DOCUMENTS : contains

    CUSTOMERS {
        uuid id PK
        uuid user_id UK "logical ref -> identity_db.users.id"
        varchar customer_number UK
        varchar status
        date date_of_birth
        varchar country_code
        timestamp created_at
        timestamp updated_at
    }
    KYC_APPLICATIONS {
        uuid id PK
        uuid customer_id FK
        varchar status
        varchar verification_level
        timestamp submitted_at
        timestamp reviewed_at
        uuid reviewed_by "logical ref -> admin_db.admin_users.id"
        text rejection_reason
        timestamp created_at
        timestamp updated_at
    }
    KYC_DOCUMENTS {
        uuid id PK
        uuid kyc_application_id FK
        varchar document_type
        varchar document_reference
        varchar status
        timestamp uploaded_at
        timestamp verified_at
    }
```

---

## 3. `wallet_db` — Wallet Service

```mermaid
erDiagram
    WALLETS ||--o{ WALLET_LIMITS : configures
    WALLETS ||--o{ DEPOSITS : records
    WALLETS ||--o{ WITHDRAWALS : records
    WALLETS ||--o{ OUTBOX_EVENTS : emits

    WALLETS {
        uuid id PK
        uuid customer_id "logical ref -> customer_db.customers.id"
        varchar wallet_number UK
        char currency
        varchar status
        numeric available_balance
        numeric reserved_balance
        bigint version "optimistic locking"
        timestamp created_at
        timestamp updated_at
    }
    WALLET_LIMITS {
        uuid id PK
        uuid wallet_id FK "UK, 1:1"
        numeric daily_transfer
        numeric daily_withdrawal
        numeric daily_deposit
        numeric single_transfer
        numeric single_withdrawal
        timestamp created_at
        timestamp updated_at
    }
    DEPOSITS {
        uuid id PK
        uuid wallet_id FK
        numeric amount
        char currency
        varchar status
        varchar reference UK
        timestamp created_at
        timestamp completed_at
    }
    WITHDRAWALS {
        uuid id PK
        uuid wallet_id FK
        numeric amount
        char currency
        varchar status
        varchar reference UK
        timestamp created_at
        timestamp completed_at
    }
    OUTBOX_EVENTS {
        uuid id PK
        varchar aggregate_type
        uuid aggregate_id
        varchar event_type
        jsonb payload
        boolean published
        timestamp created_at
    }
```

**Invariant enforced at the DB level:** `available_balance >= 0` and `reserved_balance >= 0` (CHECK constraints) — the balance itself is a snapshot column here (unlike the Ledger service's derived-from-entries model), reconciled against `ledger_db` asynchronously via `MoneyDeposited` / `MoneyWithdrawn` events.

---

## 4. `transfer_db` — Transfer Service

```mermaid
erDiagram
    BENEFICIARIES {
        uuid id PK
        uuid customer_id "logical ref -> customer_db.customers.id"
        uuid beneficiary_customer_id "logical ref -> customer_db.customers.id"
        varchar nickname
        varchar status
        timestamp created_at
        timestamp updated_at
    }
    TRANSFERS {
        uuid id PK
        uuid sender_customer_id
        uuid receiver_customer_id
        uuid sender_wallet_id "logical ref -> wallet_db.wallets.id"
        uuid receiver_wallet_id "logical ref -> wallet_db.wallets.id"
        numeric amount
        char currency
        varchar status
        varchar reference UK
        varchar idempotency_key UK
        varchar description
        text failure_reason
        timestamp created_at
        timestamp completed_at
    }
    IDEMPOTENCY_RECORDS {
        uuid id PK
        varchar idempotency_key UK
        uuid request_hash
        jsonb cached_response
        timestamp expires_at
        timestamp created_at
    }
    OUTBOX_EVENTS {
        uuid id PK
        varchar aggregate_type
        uuid aggregate_id
        varchar event_type
        jsonb payload
        boolean published
        timestamp created_at
    }
```

**Constraints of note:** `chk_transfer_amount CHECK (amount > 0)`, `chk_transfer_not_self CHECK (sender_customer_id <> receiver_customer_id)`, unique `(customer_id, beneficiary_customer_id)` pair on beneficiaries.

---

## 5. `payment_db` — Payment & Merchant Service

```mermaid
erDiagram
    MERCHANTS ||--o{ PAYMENTS : receives
    MERCHANTS ||--o{ QR_CODES : generates
    PAYMENTS ||--o{ REFUNDS : may_have

    MERCHANTS {
        uuid id PK
        uuid customer_id "logical ref -> customer_db.customers.id"
        varchar merchant_number UK
        varchar business_name
        varchar status
        timestamp created_at
        timestamp updated_at
    }
    PAYMENTS {
        uuid id PK
        uuid merchant_id FK
        uuid customer_id "logical ref -> customer_db.customers.id"
        numeric amount
        char currency
        varchar status
        varchar reference UK
        varchar payment_method
        timestamp created_at
        timestamp completed_at
    }
    QR_CODES {
        uuid id PK
        uuid merchant_id FK
        varchar qr_reference UK
        varchar status
        timestamp expires_at
        timestamp created_at
    }
    REFUNDS {
        uuid id PK
        uuid payment_id FK
        numeric amount
        text reason
        varchar status
        varchar reference UK
        timestamp created_at
        timestamp completed_at
    }
```

---

## 6. `ledger_db` — Ledger Service (system of record for money)

```mermaid
erDiagram
    FINANCIAL_TRANSACTIONS ||--o{ LEDGER_ENTRIES : posts
    LEDGER_ACCOUNTS ||--o{ LEDGER_ENTRIES : receives
    FINANCIAL_TRANSACTIONS ||--o{ REVERSALS : may_have

    LEDGER_ACCOUNTS {
        uuid id PK
        varchar account_number UK
        varchar account_type "CUSTOMER | MERCHANT | SYSTEM | FEE | SUSPENSE"
        uuid owner_reference "logical ref -> wallet_db / payment_db"
        char currency
        varchar status "ACTIVE | FROZEN | CLOSED"
        timestamp created_at
    }
    FINANCIAL_TRANSACTIONS {
        uuid id PK
        varchar transaction_reference UK
        varchar transaction_type
        varchar status
        char currency
        numeric total_amount
        timestamp created_at
        timestamp completed_at
    }
    LEDGER_ENTRIES {
        uuid id PK
        uuid financial_transaction_id FK
        uuid ledger_account_id FK
        varchar entry_type "DEBIT | CREDIT"
        numeric amount
        timestamp created_at
    }
    REVERSALS {
        uuid id PK
        uuid original_transaction_id FK
        uuid reversal_transaction_id FK
        text reason
        uuid performed_by
        timestamp created_at
    }
```

**Core invariant:** for every `financial_transaction_id`, `SUM(amount WHERE entry_type='DEBIT') = SUM(amount WHERE entry_type='CREDIT')`. Entries are append-only — corrections happen exclusively through a `REVERSALS` row plus a brand-new offsetting `FINANCIAL_TRANSACTIONS` entry, never an `UPDATE`/`DELETE` on existing entries.

*Example — transfer of 10,000 THB:*
```
Sender Account      DEBIT   10,000
Receiver Account    CREDIT  10,000
                    Balanced ✓
```

---

## 7. `risk_db` — Risk Service

```mermaid
erDiagram
    RISK_ASSESSMENTS ||--o{ FRAUD_ALERTS : may_trigger

    RISK_ASSESSMENTS {
        uuid id PK
        uuid transaction_id "logical ref -> transfer_db / payment_db"
        numeric risk_score
        varchar risk_level
        varchar decision
        text reason
        timestamp created_at
    }
    FRAUD_ALERTS {
        uuid id PK
        uuid transaction_id
        uuid risk_assessment_id FK
        varchar status
        varchar severity
        text description
        uuid assigned_to "logical ref -> admin_db.admin_users.id"
        timestamp created_at
        timestamp resolved_at
    }
```

---

## 8. `notification_db` — Notification Service

```mermaid
erDiagram
    NOTIFICATIONS {
        uuid id PK
        uuid user_id "logical ref -> identity_db.users.id"
        varchar type
        varchar channel
        varchar title
        text message
        varchar status
        timestamp read_at
        timestamp created_at
    }
    NOTIFICATION_PREFERENCES {
        uuid id PK
        uuid user_id UK "logical ref -> identity_db.users.id"
        boolean email_enabled
        boolean sms_enabled
        boolean push_enabled
        boolean transaction_alerts
        boolean security_alerts
        boolean marketing_enabled
        timestamp updated_at
    }
```

---

## 9. `admin_db` — Admin & Operations Service

```mermaid
erDiagram
    ADMIN_USERS ||--o{ USER_ROLES : assigned
    ROLES ||--o{ USER_ROLES : grants
    ROLES ||--o{ ROLE_PERMISSIONS : includes
    PERMISSIONS ||--o{ ROLE_PERMISSIONS : granted_via

    ADMIN_USERS {
        uuid id PK
        uuid user_id UK "logical ref -> identity_db.users.id"
        varchar employee_number UK
        varchar status
        timestamp created_at
        timestamp updated_at
    }
    ROLES {
        uuid id PK
        varchar name UK
        text description
    }
    PERMISSIONS {
        uuid id PK
        varchar name UK
        varchar resource
        varchar action
    }
    USER_ROLES {
        uuid user_id FK
        uuid role_id FK
    }
    ROLE_PERMISSIONS {
        uuid role_id FK
        uuid permission_id FK
    }
    AUDIT_LOGS {
        uuid id PK
        uuid actor_id
        varchar actor_type
        varchar action
        varchar resource_type
        uuid resource_id
        jsonb old_value
        jsonb new_value
        inet ip_address
        uuid correlation_id
        timestamp created_at
    }
    APPROVALS {
        uuid id PK
        varchar request_type
        varchar resource_type
        uuid resource_id
        uuid requested_by
        uuid approved_by
        varchar status
        text reason
        timestamp created_at
        timestamp approved_at
    }
```

**Maker-Checker pattern** for sensitive operations: Admin A raises an `APPROVALS` row → Admin B reviews and approves/rejects. No single admin can unilaterally execute a sensitive action (e.g. force-unfreeze a wallet).

---

## 10. Cross-Service Logical Reference Map

Since there are no physical foreign keys between services, this table is the source of truth for how records relate across the platform. Consumers must resolve these via API call or the event payload that originally carried the ID — never via direct DB query.

| From (service.table.column) | Logically references | Resolved via |
|---|---|---|
| `customer_db.customers.user_id` | `identity_db.users.id` | `CustomerCreated` event carries `userId` |
| `wallet_db.wallets.customer_id` | `customer_db.customers.id` | `CustomerCreated` / `KycApproved` event |
| `transfer_db.transfers.sender_wallet_id` | `wallet_db.wallets.id` | Transfer Service calls Wallet Service API at transfer time |
| `ledger_db.ledger_accounts.owner_reference` | `wallet_db.wallets.id` or `payment_db.merchants.id` | `WalletCreated` / `MerchantApproved` event |
| `risk_db.fraud_alerts.assigned_to` | `admin_db.admin_users.id` | Admin Service API (assignment action) |
| `notification_db.notifications.user_id` | `identity_db.users.id` | Carried in every domain event's `userId`/`customerId` field |
| `admin_db.audit_logs.actor_id` | `identity_db.users.id` or `admin_db.admin_users.id` | Carried in request context / JWT claims |

---

## 11. High-Level Cross-Domain Overview

```mermaid
erDiagram
    USERS ||--o| CUSTOMERS : "becomes (cross-service)"
    CUSTOMERS ||--o{ KYC_APPLICATIONS : submits
    CUSTOMERS ||--o{ WALLETS : owns
    WALLETS ||--o{ DEPOSITS : has
    WALLETS ||--o{ WITHDRAWALS : has
    WALLETS ||--o{ WALLET_LIMITS : configures
    CUSTOMERS ||--o{ TRANSFERS : sends
    WALLETS ||--o{ LEDGER_ACCOUNTS : "maps to (cross-service)"
    LEDGER_ACCOUNTS ||--o{ LEDGER_ENTRIES : records
    CUSTOMERS ||--o{ MERCHANTS : "may become"
    MERCHANTS ||--o{ PAYMENTS : receives
    PAYMENTS ||--o{ REFUNDS : may_have
    TRANSFERS ||--o{ FRAUD_ALERTS : "may trigger (cross-service)"
```

This diagram spans 9 physical databases — it exists purely to communicate the platform's logical data model to reviewers, and does **not** represent an executable schema or a shared database.
